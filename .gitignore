# Secrets / local env
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
*.pem
*.key

# Cloudflare build artifacts
.wrangler/

# Node (only used locally for the in-browser JSX transpile check)
node_modules/

# OS cruft
.DS_Store
Thumbs.db
